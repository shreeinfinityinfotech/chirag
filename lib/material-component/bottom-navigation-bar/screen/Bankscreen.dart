import 'package:flutter/material.dart';

void main() => runApp(bankscreen());

class bankscreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text("bankscreen"),
      ),
    );
  }
}
