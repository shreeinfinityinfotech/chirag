import 'package:flutter/material.dart';

void main() => runApp(Profilescreen());

class Profilescreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text("Profile Scrren"),
      ),
    );
  }
}
